# Mosquitto as the CI job `mosquitto` runs it, for the ^:portable tests.
# Defaults otherwise, so a divergence is Mosquitto's stock behaviour and not
# something configured into it.
listener 1883
allow_anonymous true
log_type all
